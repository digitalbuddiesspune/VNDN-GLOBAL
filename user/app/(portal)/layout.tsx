import { AccountPortalLayout } from "@/components/portal/AccountPortalLayout";

export default function PortalLayout({ children }: { children: React.ReactNode }) {
  return <AccountPortalLayout>{children}</AccountPortalLayout>;
}
